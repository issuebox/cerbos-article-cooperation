---
apiVersion: api.cerbos.dev/v1
resourcePolicy:
  version: default
  scope: "user"
  resource: organization
  rules:
    - actions:
        - delete_user
      effect: EFFECT_DENY
      roles:
        - admin
      condition:
        match:
          expr: request.resource.attr.role == 'owner'

