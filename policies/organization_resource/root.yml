---
apiVersion: api.cerbos.dev/v1
resourcePolicy:
  version: default
  resource: organization
  rules:
    - actions:
        - "*"
      effect: EFFECT_ALLOW
      roles:
        - owner
        - admin

