---
apiVersion: api.cerbos.dev/v1
resourcePolicy:
  version: default
  resource: channel
  rules:
    - actions:
        - "*"
      effect: EFFECT_ALLOW
      roles:
        - owner
        - admin

